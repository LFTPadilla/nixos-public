---
services:
  webtop:
    image: lscr.io/linuxserver/webtop:fedora-i3
    container_name: webtop
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - /home/felipe/server/fedora:/config
    ports:
      - 3000:3000
      - 3002:3001
    shm_size: "1gb"
    restart: unless-stopped
